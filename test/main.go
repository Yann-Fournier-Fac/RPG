package main

// import "ebiten"

import (
	"fmt"
	"rpg"
)

//"rpg"

func main() {
	slt := rpg.Red + "Salut" + rpg.Reset
    fmt.Printf(slt)
	/*var enemi rpg.PNJ
	enemi.Name = "Darkvador"
	enemi.Attaque = 25
	enemi.LP = 100
	resultat := rpg.Fight(player, enemi)
	fmt.Println(resultat)*/

	//f0, nb0 := rpg.Floor0()
	/*f1, nb1 := rpg.Floor1()
	f2, nb2 := rpg.Floor2()
	f3, nb3 := rpg.Floor3()
	rpg.Map(f0, nb0)
	rpg.Map(f1, nb1)
	rpg.Map(f2, nb2)
	rpg.Map(f3, nb3)*/
	//rpg.Moove(f0, nb0, player)

}
